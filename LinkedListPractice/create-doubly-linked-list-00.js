class ListNode {
  /**
   * @param {number} val
   * @param {Node} next
   * @param {Node} prev
   */
  constructor(val, next = null, prev = null) {}
}

/**
 * @param {number[]} nums
 * @return {Node}
 */
function createDoublyLinkedList(nums) {}

let nums, result;
nums = [1, 2, 3, 4];
result = createDoublyLinkedList(nums);
result;
